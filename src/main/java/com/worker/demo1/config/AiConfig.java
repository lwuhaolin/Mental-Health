package com.worker.demo1.config;

import org.springframework.context.annotation.Configuration;

/**
 * AI配置类 - 使用HTTP客户端直接调用通义千问API
 */
@Configuration
public class AiConfig {
    // 使用HTTP客户端直接调用API，无需Spring AI依赖
}